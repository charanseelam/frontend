# Replace with your deployed backend URL in Vercel Environment Variables
# The app reads `import.meta.env.VITE_API_URL` — use this name below.
VITE_API_URL=https://backend-production-5d72.up.railway.app/api

# (Legacy) If you see `VITE_API_BASE_URL` in docs, it's interchangeable — prefer `VITE_API_URL`.
